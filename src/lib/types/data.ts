type textDataType = {
  title?: string;
  text: string;
};

type linkDataType = {
  text: string;
  link: string;
};

type imageDataType = {
  src: string;
  alt: string;
};

export type { imageDataType, linkDataType, textDataType };
