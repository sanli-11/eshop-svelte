import type { langOption } from "$lib/types/langOption";

const languages: langOption[] = [
  { id: "1", flag: '<span class="fi fi-gb fis"></span>', language: "English" },
  { id: "0", flag: '<span class="fi fi-pk fis"></span>', language: "Urdu" },
];

export default languages;
